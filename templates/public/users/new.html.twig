{% extends 'public/public.html.twig' %}

{% block title %}New Users{% endblock %}

{% block container %}
    <div class="container p-5 mx-auto">
        <h1 class="text-center mt-4 mb-4">Inscription</h1>
        <div class="col-sm-12 col-lg-6 border border-dark p-5 mx-auto backgroundCard borderCo">
    {{ form_start(form) }}
            <div class="col">
                <div class="row mb-3">
                    <label class="descrip" for="nom">Nom :</label>
                    {{ form_widget(form.nom) }}
                </div>
                <div class="row mb-3 mt-3">
                    <label class="descrip" for="prenom">Prénom :</label>
                    {{ form_widget(form.prenom) }}
                </div>
                <div class="row mb-3 mt-3">
                    <label class="descrip" for="pseudo">Pseudo :</label>
                    {{ form_widget(form.pseudo) }}
                </div>
                <div class="row mb-3 mt-3">
                    <label class="descrip" for="email">Email :</label>
                    {{ form_widget(form.email) }}
                </div>
                <div class="row mt-3">
                    <label class="descrip" for="password">Mot de Passe :</label>
                    {{ form_widget(form.password) }}
                </div>
            </div>
            <div class="text-right pr-0 mt-5">
                <button class="btn btn-dark text-white">{{ button_label|default('Inscription') }}</button>
            </div>
    {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
