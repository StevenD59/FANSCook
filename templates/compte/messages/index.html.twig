{% extends 'base.html.twig' %}

{% block title %}Messages index{% endblock %}

{% block body %}
    <h1>Messages index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Objet</th>
                <th>Lu</th>
                <th>Message</th>
                <th>Date_add</th>
                <th>Date_delete</th>
                <th>Activate</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for message in messages %}
            <tr>
                <td>{{ message.id }}</td>
                <td>{{ message.email }}</td>
                <td>{{ message.objet }}</td>
                <td>{{ message.lu ? 'Yes' : 'No' }}</td>
                <td>{{ message.message }}</td>
                <td>{{ message.dateAdd ? message.dateAdd|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ message.dateDelete ? message.dateDelete|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ message.activate ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('messages_show', {'id': message.id}) }}">show</a>
                    <a href="{{ path('messages_edit', {'id': message.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('messages_new') }}">Create new</a>
{% endblock %}
