{% extends 'admin/admin.html.twig' %}

{% block title %}Edit Users{% endblock %}

{% block container %}
    <div class="card-body col-lg-8 m-auto">
        <h1 class="text-center mb-3 ">Modifier utilisateur</h1>


        <div class="container col-5 col-xl-5">
            {{ form_start(form) }}
            <div>
                <div class="border border-dark p-3 backgroundFieldset">
                    <div class="form-row">
                        <div class="form-group col-md-6">
                            <label class="descrip"  for="nom">Nom:</label>
                            {{ form_widget(form.nom) }}
                        </div>
                        <div class="form-group col-md-6 ">
                            <label class="descrip"  for="prenom">Prénom:</label>
                            {{ form_widget(form.prenom) }}
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="descrip" for="pseudo">Pseudo:</label>
                        {{ form_widget(form.pseudo) }}
                    </div>
                    <div class="form-group">
                        <label class="descrip" for="mail">E-mail:</label>
                        {{ form_widget(form.email) }}
                    </div>
                    <div class="text-right mt-3">
                        <button class="btn btn-dark">{{ button_label|default('Modifier') }}</button>
                    </div>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>



    <a href="{{ path('admin_users_index') }}">back to list</a>


{% endblock %}